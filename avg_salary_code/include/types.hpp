// typedef __uint128_t Lint; // for ring size in [63,126];
typedef unsigned long long Lint; // for ring size in [1,62]; change to unsigned
// typedef unsigned  Lint; // for ring size in [1,62]; change to unsigned

// struct poisson_result {
//      int zero_point;
//      float scale;
//      poisson_result() {
//           // scale = _scale;
//           // zero_point = _zero_point;
//      }
//      poisson_result(float _scale, int _zero_point) {
//           scale = _scale;
//           zero_point = _zero_point;
//      }
// };
